services:
  - type: web
    name: wex-theory
    runtime: python
    buildCommand: pip install -r requirements.txt && python migrate_stripe.py; python import_data.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
